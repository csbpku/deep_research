{
  "name": "deep_research",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "engines": {
    "node": ">=20.11.0",
    "pnpm": ">=10"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/client",
      "@prisma/engines",
      "esbuild",
      "prisma",
      "sharp"
    ]
  },
  "scripts": {
    "db:migrate": "pnpm --filter @deep-research/web db:migrate",
    "db:generate": "pnpm --filter @deep-research/web db:generate",
    "db:reset": "pnpm --filter @deep-research/web db:reset",
    "dev:web": "pnpm --filter @deep-research/web dev",
    "dev:ai": "cd packages/ai-engine && UV_CACHE_DIR=${UV_CACHE_DIR:-/tmp/deep-research-uv-cache} uv run uvicorn ai_engine.server.app:app --port 4000 --reload",
    "ingest:summaries": "cd packages/ai-engine && UV_CACHE_DIR=${UV_CACHE_DIR:-/tmp/deep-research-uv-cache} uv run python -m ai_engine.ingestion.pipeline",
    "worker:imports": "cd packages/ai-engine && UV_CACHE_DIR=${UV_CACHE_DIR:-/tmp/deep-research-uv-cache} uv run python -m ai_engine.import_worker",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
