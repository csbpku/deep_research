# apps/web/prisma

> 数据库 schema 与迁移。**Week 0 锁定后工程师 A/B 不直接改**。
> 修改必须开 `[db]` PR，主会话 review；架构变更须同步更新
> `docs/ARCHITECTURE.md` §五 / `docs/IMPLEMENTATION_PLAN.md`。

schema 与 seed 由 Task #9（建 prisma/schema.prisma）落地。
